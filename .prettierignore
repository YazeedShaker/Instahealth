node_modules
.next
.expo
.turbo
dist
build
coverage
playwright-report
test-results
pnpm-lock.yaml
apps/mobile/ios
apps/mobile/android
Pre-docs
Implementation Docs
UI screens
# Claude Design handoff bundles — exported prototypes, not our code style.
# There is exactly ONE bundle: design/handoff/project (see design/handoff/
# EXPORT.md). Without this, Prettier reformats an exported prototype we keep
# verbatim and the format gate fails.
design/*/project
supabase/migrations
supabase/functions
packages/core/src/types/database.ts

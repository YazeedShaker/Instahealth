# design/dashboard

Claude Design handoff bundles for the **provider dashboard** (P-series).

The DESIGN-02 handoff lands here; P02+ screens go in alongside it. One folder
per handoff, exactly as `design/mobile/*` is organised.

_Empty until DESIGN-02 is exported — this file only keeps the folder in git._

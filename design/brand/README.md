# design/brand

The brand assets: the mark's SVG masters in `logo/`, and **[BRAND.md](./BRAND.md)**
— the rules, transcribed from the source bundle with the gaps marked as gaps.

⚠ The source, `InstaHealth Brand Assets.html` (1.6 MB), is **deliberately not
committed** — founder's instruction. `BRAND.md` is the extract the code needs;
re-read the bundle for anything it does not cover, and add what you find there
rather than deciding it here.
